const themes = {
  CUSTOM: "custom",
  DARK: "dark",
  DEFAULT: "default",
};

// Key to store theme preference locally
const THEME_PREF_KEY = "theme_pref_local";

export { themes as default, THEME_PREF_KEY };
